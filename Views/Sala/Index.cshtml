@model List<CineBenavides.Models.Sala>
@{ ViewData["Title"] = "Salas"; }

<div class="container mt-4">
    @if (TempData["Mensaje"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["Mensaje"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <h2 class="mb-4">Salas</h2>

    @if (Context.Session.GetString("Rol") == "Administrador")
    {
        <a class="btn btn-success mb-3" href="/Sala/Create">+ Nueva Sala</a>
    }

    @if (!Model.Any())
    {
        <p class="text-muted">No hay salas registradas aún.</p>
    }

    <table class="table table-striped table-bordered">
        <thead class="table-dark">
            <tr>
                <th>Nombre</th>
                <th>Capacidad</th>
                <th>Asientos generados</th>
                @if (Context.Session.GetString("Rol") == "Administrador")
                {
                    <th>Acciones</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Nombre</td>
                    <td>@item.Capacidad</td>
                    <td>@item.Asientos.Count</td>
                    @if (Context.Session.GetString("Rol") == "Administrador")
                    {
                        <td>
                            <a class="btn btn-warning btn-sm" href="/Sala/Edit/@item.Id">Editar</a>
                            <form action="/Sala/Delete/@item.Id" method="post" style="display:inline"
                                  onsubmit="return confirm('¿Eliminar esta sala y todas sus funciones?')">
                                <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                            </form>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
</div>
